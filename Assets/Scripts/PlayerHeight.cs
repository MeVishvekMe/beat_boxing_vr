using UnityEngine;

public class PlayerHeight : MonoBehaviour {
    public static float PlayerHeightValue;

    public static void SetPlayerHeight(float height) {
        PlayerHeightValue = height;
    }
}
